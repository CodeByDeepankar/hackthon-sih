/** @type {import('next').NextConfig} */
// Service worker placed in public/sw.js and registered manually.
// If later using next-pwa custom build integration, adjust here.
const nextConfig = {
	experimental: {},
};

export default nextConfig;
